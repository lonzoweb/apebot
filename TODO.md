# Bot TODO List

## Pending Alias Removals

- [ ] **Remove `.tc` alias from `.pull` command** — Added ~2026-03-19. Give users a few weeks to transition, then remove the `aliases=["tc"]` from `tarot_cog.py` line with the TODO comment.

## Future Improvements

- [ ] **TODO: Migrate all cooldown time settings to dashboard** — Currently hardcoded in cogs. Should be configurable from the dashboard Admin/Misc tab.
